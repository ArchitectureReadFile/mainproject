[lint]
select = [
    "E",   # pycodestyle 에러
    "F",   # pyflakes (미사용 import, 변수 등)
    "I",   # isort (import 순서)
]
ignore = [
    "E501",  # 줄 길이 (line-length로 대신 관리)
]

[lint.isort]
known-first-party = ["routers", "services", "repositories", "schemas", "models", "errors"]
